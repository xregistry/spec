markdown
bs4 
tenacity
aiohttp==3.11.12
tqdm
pymdown-extensions
pytest-asyncio
jsonpointer
pytest
pyyaml
avro
# The format extra installs the validators that make `format` enforced instead
# of annotated, which the generated URI/date-time assertions rely on. The
# nongpl variant avoids the GPL-licensed rfc3987 the plain format extra pulls.
jsonschema[format-nongpl]
openapi-spec-validator>=0.7.1
